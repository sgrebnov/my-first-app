name: 'sgrebnov."playground-app".my_custom_view'
type: append
migrations:
  - name: create_view
    sql: |
      CREATE OR REPLACE VIEW sgrebnov."playground-app".my_custom_view AS
      SELECT * FROM eth.recent_transactions
